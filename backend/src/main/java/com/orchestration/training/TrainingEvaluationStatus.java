package com.orchestration.training;

/** Evaluation is kept separate from submission: an answer remains recoverable if a provider is unavailable. */
public enum TrainingEvaluationStatus { COMPLETED, FAILED }
