.env
.env.*
!.env.example
# Claude Code 런타임 파일(스케줄 락 등) — 로컬 상태라 추적하지 않는다
.claude/
# md 동기화 로컬 오버라이드(OBSIDIAN_SYNC_DEST 등) — 사용자마다 경로가 달라서 커밋 안 함
scripts/md-sync.local
**/node_modules/
**/dist/
**/target/
*.tsbuildinfo
.DS_Store
originals/**
!originals/.gitkeep
obsidian/**
!obsidian/.gitkeep
backups/**
!backups/.gitkeep
backup-obsidian-migration/
# Coin_Trade 레포로 분리됨 — 여기서는 추적하지 않음. 앞에 슬래시를 꼭 붙여야 루트의 trading/만
# 제외되고, backend/.../com/orchestration/trading/ 같은 하위 패키지까지 같이 무시되지 않는다
# (실제로 이 슬래시가 없어서 그 백엔드 패키지 전체가 지금까지 git에 한번도 안 잡히고 있었다).
/trading/
# 틱 아카이브 원본 — 계속 커지는 데이터라 git으로 안 다룬다(NAS로 백업, scripts/backup-tick-archive.sh 참고)
/trading-tick-archive/
